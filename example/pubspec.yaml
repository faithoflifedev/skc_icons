name: skc_icon_list
description: "A new Flutter project."
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ^3.7.2

dependencies:
  collection: ^1.19.1
  go_router: ^15.0.0
  skc_icons:
    path: ../
  url_launcher: ^6.3.1
    
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
